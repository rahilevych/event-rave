export interface EventType {
  id: number;
  title: string;
  description: string;
  date: string;
  city: string;
  venue: string;
  imageUrl: string;
  organizer: string;
  price: number;
  likedByUser: boolean;
}
